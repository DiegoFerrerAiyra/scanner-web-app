import { SearchFeaturePipe } from '../pipes/search-feature.pipe';

describe('SearchFeaturePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFeaturePipe();
    expect(pipe).toBeTruthy();
  });
});
