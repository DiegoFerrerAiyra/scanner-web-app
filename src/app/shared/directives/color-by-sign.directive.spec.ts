import { ColorBySignDirective } from './color-by-sign.directive';

describe('ColorBySignDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorBySignDirective();
    expect(directive).toBeTruthy();
  });
});
